<li class="nav-item dropdown">
    <a class="nav-link animated-underscore" href="#">About</a>
    <!-- Dropdown menu -->
    <div class="dropdown-menu mt-0" aria-labelledby="navbarDropdown" style="position: absolute; inset: 0px auto auto 0px;;" data-popper-placement="bottom-start" data-mdb-popper="null">
        <div class="dropdown-container">
            <div class="row my-3 d-flex">
            {{#get "pages" filter="slug:~^'about-'" limit="all"}}
                    {{#foreach pages}}
                        <div class="col-md-12 col-lg-4 mb-3 mb-lg-0">
                            <div class="pt-2">
                                
                                    <i class="fas fa-caret-right pe-2" aria-hidden="true"></i>
                                    <a href="{{url}}">
                                        <h5 class="text-uppercase font-weight-bold">{{title}}</h5>
                                    </a>
                                
                                {{#if excerpt}}
                                    <p>{{excerpt}}</p>                                    
                                {{/if}}
                            </div>
                        </div>
                    {{/foreach}}
                {{/get}}
            </div>
        </div>
    </div>
</li>
