
{{!-- copy-and-paste from the source code of ghost default navigation helper --}}
<ul class="nav">
    {{!-- ignore navigation, use megamenu instead --}}
    {{#foreach navigation}}
    {{/foreach}}

    {{!-- select and list all page with internal tag: "hash-megamenu" (an internal tag is of the form "#tag" in editor) --}}
    {{!-- get one megamenu at a time to allow custom ordering of nav tabs --}}
    {{#get "pages" filter="slug:megamenu-for-about"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}

    {{#get "pages" filter="slug:megamenu-for-china-maps"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}

    {{#get "pages" filter="slug:megamenu-for-research"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}

    {{#get "pages" filter="slug:megamenu-for-policy"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}

    {{#get "pages" filter="slug:megamenu-for-education"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}

    {{#get "pages" filter="slug:megamenu-for-events"}}
        {{#foreach pages}}  
            {{content}}
        {{/foreach}}
    {{/get}}


</ul>

